# conda
```
conda -V
conda list

conda env list
conda info --envs
conda info --envis

conda create -n vnpy_run virtualenvwrapper-win
conda create -n vnpy_run --clone root
conda create -n vnpy_myweb

conda install -n vnpy_run pip
conda install pip

conda activate vnpy_run
conda deactivate
conda remove --name vnpy_run

python .\run.py
```
# nginx
```
start nginx
./nginx -s stop
```